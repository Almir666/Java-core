package Algorithms.LeetCode.Two_pointers;

// Linked List Cycle

// Дана head - глава связного списка, определите, есть ли в этом связном списке цикл.
// Цикл в связанном списке существует, если в нем есть некоторый узел, до которого можно добраться, постоянно следуя за следующим указателем. 
// Внутри цикла pos используется для обозначения индекса узла, с которым связан следующий указатель tail. 
// Обратите внимание, что pos не передается в качестве параметра.
// Возвращает true, если в связном списке есть цикл. В противном случае возвращается false.

public class Task11 {
    class ListNode {
        int val;
        ListNode next;

        ListNode(int x) {
            val = x;
            next = null;
        }
    }

    public boolean hasCycle(ListNode head) {
        



        return true;
    }
}
